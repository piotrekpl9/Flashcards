@model IEnumerable<Flashcards.DTOs.DeckDto>
<h2>Decki gracza: @ViewBag.DeckOwnerName</h2>
<ul>
    @foreach (var deck in Model)
    {
        <li>
            @deck.Name - 
            <a href="/Flashcard?deckId=@deck.Id">Fiszki</a>
            <a href="/DeckSession/decks/@deck.Id">Rozpocznij sesję</a>
        </li>
    }
</ul>