<ul>
    @foreach (var deck in Model)
    {
        <li>
            @deck.Name -
            <form asp-action="ApproveDeck" asp-route-id="@deck.Id" method="post" style="display:inline;">
                <button type="submit" onclick="return confirm('Are you sure you want to confirm this deck?');">zatwierdz</button>
            </form>
            <form asp-action="RejectDeck" asp-route-id="@deck.Id" method="post" style="display:inline;">
                <button type="submit" onclick="return confirm('Are you sure you want to delete this deck?');">odrzuć</button>
            </form>
            <a href="/Flashcard?deckId=@deck.Id">Flashcards</a>
        </li>
    }
</ul>