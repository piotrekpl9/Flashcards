@model IEnumerable<Flashcards.DTOs.FlashcardDto>

<a href="/Flashcard/new">Dodaj</a>

<ul>
    @foreach (var flashcard in Model)
    {
        <li>
            @flashcard.Front - 
            <a href="/Flashcard/edit/@flashcard.Id">Edycja</a>
            <form asp-action="DeleteFlashcard" asp-route-id="@flashcard.Id" method="post" style="display:inline;">
                <button type="submit" onclick="return confirm('Are you sure you want to delete this deck?');">Usuń</button>
            </form>
        </li>
    }
</ul>

<a href="/Deck">Powrót</a>